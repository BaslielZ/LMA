<?xml version="1.0" encoding="utf-8" ?>
<ContentPage 
    xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
    xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
    xmlns:viewModels="clr-namespace:LifeManagementApp.ViewModels"
    xmlns:models="clr-namespace:LifeManagementApp.Models"
    x:Class="LifeManagementApp.Views.AllNotesPage"
    x:DataType="viewModels:NotesViewModel"
    Title="Your Notes">

    <ContentPage.ToolbarItems>
        <ToolbarItem Text="Add" 
                     Command="{Binding NewCommand}" 
                     IconImageSource="{FontImage Glyph='+', Color=Black, Size=22}" />
    </ContentPage.ToolbarItems>

    <!-- Single root content -->
    <ScrollView>
        <VerticalStackLayout Padding="20" Spacing="20">

            <!-- Jokes Section -->
            <Label Text="Joke of the Day"
                   FontSize="20"
                   FontAttributes="Bold" />

            <CollectionView ItemsSource="{Binding Jokes}">
                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:Joke">
                        <Frame BackgroundColor="LightGoldenrodYellow"
                               CornerRadius="10"
                               Padding="15"
                               HasShadow="True">
                        <Label Text="{Binding Content}"
                               FontSize="16"
                               Margin="0,5" />
                        </Frame>
                    </DataTemplate>
                </CollectionView.ItemTemplate>
            </CollectionView>

            <!-- Notes Section -->
            <Label Text="Your Notes"
                   FontSize="20"
                   FontAttributes="Bold"
                   Margin="0,20,0,0" />

            <CollectionView 
                x:Name="notesCollection"
                ItemsSource="{Binding AllNotes}"
                SelectionMode="Single"
                SelectionChanged="OnSelectionChanged">
                <CollectionView.ItemsLayout>
                    <LinearItemsLayout Orientation="Vertical" ItemSpacing="10"/>
                </CollectionView.ItemsLayout>

                <CollectionView.ItemTemplate>
                    <DataTemplate x:DataType="models:DbNote">
                        <StackLayout Padding="15">
                            <Label Text="{Binding Title}" 
                                   FontSize="21" 
                                   TextColor="Black"/>

                            <Label Text="{Binding Text}" 
                                   FontSize="17" 
                                   TextColor="Gray"/>

                            <Label Text="{Binding CreatedAt, StringFormat='Created: {0:yyyy-MM-dd HH:mm}'}" 
                                   FontSize="13"
                                   TextColor="HotPink"/>
                        </StackLayout>

                    </DataTemplate>
                </CollectionView.ItemTemplate>

            </CollectionView>

        </VerticalStackLayout>
    </ScrollView>

</ContentPage>
